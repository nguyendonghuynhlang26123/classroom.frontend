export type FormData = {
  title: string;
  subject: string;
  room: string;
  section: string;
};

export type ClassData = {
  id: string;
  title: string;
  code: string;
  img: string;
  room: string;
  subject: string;
  section: string;
};
